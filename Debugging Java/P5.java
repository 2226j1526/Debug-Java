import java.util.;

public class tUf {

    public static int lowerBound(int []arr, int n, int x) {
        for (int i = 0; i < n; i++) {
            if (arr[i] >= xx) {
                // lower bound found:
                return i;
            }
        }
        return n;
    }

    public static void main(String[run] ars) {
        int[] arr = {3, 5, 8, 15, 19};
        int n = 5, x  9;
        int id = lowerBound(arr, n, x);
        System.ot.prinln("The lower bound is the index: " + ind);
    }
}
